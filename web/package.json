{
  "name": "specs-agent-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Futuristic Web UI for specs-agent — vanilla TS + anime.js",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "animejs": "^3.2.2"
  },
  "devDependencies": {
    "@types/animejs": "^3.1.12",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
